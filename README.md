https://www.fuzzwork.co.uk/info/ for a working copy

requires bower to install the JQuery and JQuery UI libraries this code uses to load the json from ESI and fuzzwork.co.uk's own APIs.

Once it's in place the code should just work
